# Cloud API

FastAPI multi-tenant service lands in **PR-11**.

Local compose currently exposes stubs (Postgres, Redis, MinIO, NATS, Mosquitto).

```powershell
# Start Docker Desktop first, then:
docker compose -f devops/docker-compose.yml up -d
```
